<h1>Editing User Group</h1>

<%= form_for @user_group, url: user_group_path(@user_group), method: :put do |f| %>
    <%= render partial: 'form', locals: { f: f } %>
<% end %>

<%= link_to 'Show', user_group_path(@user_group) %> |
<%= link_to 'Back', user_groups_path %>
