<% if user_signed_in? and current_user.user_group.name != 'Visitor' %>
    <li><%= user_link('nav-link') %></li>
    <li><%= link_to t('main_menu.disconnect'), destroy_user_session_path, :method => :delete %></li>
<% else %>
    <li><%= link_to t('main_menu.connect'), user_session_path %></li>
    <li><%= link_to t('main_menu.register'), new_user_registration_path %></li>
<% end %>